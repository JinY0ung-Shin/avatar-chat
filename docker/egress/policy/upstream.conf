# If this host reaches the internet only through a corporate HTTP proxy:
# cache_peer proxy.corp.example parent 8080 0 no-query default
# never_direct allow all
# Prefer an unauthenticated proxy restricted to this server's source address.
# If authentication is required, keep credentials in this host-only file.
